imports:
    - { resource: config.yml }

framework:
    router:
        resource: '%kernel.project_dir%/app/config/routing_dev.yml'
        strict_requirements: true
    profiler: { only_exceptions: false }

web_profiler:
    toolbar: true
    intercept_redirects: false

monolog:
    handlers:
        main:
            type: stream
            path: '%kernel.logs_dir%/%kernel.environment%.log'
            level: debug
            channels: ['!event']
        console:
            type: console
            process_psr_3_messages: false
            channels: ['!event', '!doctrine', '!console']
        # To follow logs in real time, execute the following command:
        # `bin/console server:log -vv`
        server_log:
            type: server_log
            process_psr_3_messages: false
            host: 127.0.0.1:9911
        # uncomment to get logging in your browser
        # you may have to allow bigger header sizes in your Web server configuration
        #firephp:
        #    type: firephp
        #    level: info
        #chromephp:
        #    type: chromephp
        #    level: info

#swiftmailer:
#    delivery_addresses: ['me@example.com']
#c'est l'adresse qu'on active en mode développement
swiftmailer:
    delivery_addresses: ['22f484ffb1-7034e3@inbox.mailtrap.io', 'philip.dang.pro@gmail.com']
    spool:     { type: memory }
    transport: smtp
    host:      smtp.mailtrap.io
    username:  42a0e3dff39870
    password:  1b0c5dad3ec01c
    auth_mode: cram-md5
    port: 2525


parameters:
      database_name: commerce
      database_user: root
      database_password: troiswa